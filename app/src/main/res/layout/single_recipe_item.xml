<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:cardview="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:id="@+id/cv_single_recipe"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    cardview:cardBackgroundColor="@color/primary"
    cardview:cardCornerRadius="@dimen/card_view_corner_radius"
    cardview:cardElevation="@dimen/card_view_elevation"
    cardview:cardUseCompatPadding="true"
    android:layout_margin="@dimen/recipe_list_margin"
    android:foreground="?android:attr/selectableItemBackground">

<RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/iv_single_recipe_image"
            android:layout_width="match_parent"
            android:layout_height="@dimen/single_recipe_image_height"
            android:contentDescription="@string/single_recipe_image_content_description"
            android:gravity="center"
            android:scaleType="centerCrop"
            android:src="@mipmap/ic_launcher" />

        <TextView
            android:id="@+id/tv_single_recipe_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/iv_single_recipe_image"
            android:background="@color/primary"
            android:fontFamily="sans-serif"
            android:gravity="center"
            android:padding="@dimen/grid_text_padding"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium"
            android:textColor="@color/primary_text"
            tool:text="Recipe Name"/>
    </RelativeLayout>
</android.support.v7.widget.CardView>